# Datos crudos y procesados (públicos, no versionar por tamaño)
raw_data/
processed_data/
*.csv
*.parquet
*.pkl

# EXCEPCIÓN: dataset analítico comprimido (~25 MB) que SÍ va al repo
!data/
!data/dataset_tesis_clean.parquet

# Modelos entrenados y artefactos generados por los scripts
codigo/
models/
catboost_info/
*.cbm
*.bin
*.npy

# Entorno
venv/
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Figuras generadas (se reproducen desde los scripts)
results/
*.png
